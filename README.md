# FindMissingPlexFiles
This tool is a simple to use just run PlexMissingMedia_GUI.exe it used lite3.exe backend that searches for missing files from you plex libary if the drive gets deleted or breaks
